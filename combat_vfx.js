// MAKYREN-016 — procedural weapon presentation, muzzle flash, impact feedback, and combat animation hooks.
export function createWeaponVFX(scene){const flash=new THREE.Mesh(new THREE.SphereGeometry(.09,8,8),new THREE.MeshBasicMaterial({color:0xffd36a}));flash.visible=false;scene.add(flash);const impact=new THREE.Mesh(new THREE.RingGeometry(.04,.16,12),new THREE.MeshBasicMaterial({color:0xff8a3d,transparent:true,opacity:.9,side:THREE.DoubleSide}));impact.visible=false;scene.add(impact);return{flash,impact,fire(position){flash.position.copy(position);flash.scale.setScalar(.7+Math.random()*.6);flash.visible=true;setTimeout(()=>flash.visible=false,55)},hit(position){impact.position.copy(position);impact.visible=true;impact.scale.setScalar(1);setTimeout(()=>impact.visible=false,120)}}}
export function createWeaponModel(type='pistol'){const g=new THREE.Group();const metal=new THREE.MeshStandardMaterial({color:0x15191e,metalness:.82,roughness:.24});const grip=new THREE.MeshStandardMaterial({color:0x17110e,roughness:.8});const body=new THREE.Mesh(new THREE.BoxGeometry(.18,.18,.72),metal);body.position.z=-.28;g.add(body);const handle=new THREE.Mesh(new THREE.BoxGeometry(.16,.32,.18),grip);handle.position.set(0,-.22,-.02);handle.rotation.x=-.18;g.add(handle);if(type==='smg'){const stock=new THREE.Mesh(new THREE.BoxGeometry(.18,.15,.38),metal);stock.position.z=.22;g.add(stock)}g.userData.weaponType=type;return g}
export function attachCombatAnimationHooks(character){character.userData.combatAnimation={state:'idle',setState(s){this.state=s;character.userData.combatState=s},update(dt){if(this.state!=='idle')character.userData.combatTimer=(character.userData.combatTimer||0)+dt}};return character.userData.combatAnimation}
window.MakyrenCombatVFX={createWeaponVFX,createWeaponModel,attachCombatAnimationHooks};