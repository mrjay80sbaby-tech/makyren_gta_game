const missionEl=document.querySelector('.mission');const cashEl=document.getElementById('cash');const heat=document.createElement('div');heat.id='heat';heat.style.cssText='position:fixed;top:58px;right:20px;padding:7px 10px;border:1px solid #ffffff1a;border-radius:8px;background:#080b10cc;font-weight:900;font-size:12px;letter-spacing:.08em';document.body.appendChild(heat);let cash=500,wanted=0,state='find',cooldown=0;const saveKey='makyren-local-save';try{const s=JSON.parse(localStorage.getItem(saveKey)||'{}');cash=Number.isFinite(s.cash)?s.cash:500;wanted=Number.isFinite(s.wanted)?s.wanted:0}catch(e){}function render(){cashEl.textContent=`$${cash}`;heat.textContent=`HEAT ${wanted}/5 ${'★'.repeat(wanted)}${'☆'.repeat(5-wanted)}`;heat.style.color=wanted?'#ffd15c':'#9bb1c8';if(state==='find')missionEl.innerHTML='<span>MISSION 01</span><b>STEAL A RIDE</b><small>Find a vehicle and press ENTER / EXIT</small>';else if(state==='escape')missionEl.innerHTML='<span>WANTED</span><b>LOSE THE HEAT</b><small>Keep moving until the heat drops</small>';else missionEl.innerHTML='<span>COMPLETE</span><b>FIRST RUN</b><small>+$500 • New missions unlocked</small>'}function persist(){localStorage.setItem(saveKey,JSON.stringify({version:1,cash,wanted}))}function reward(){cash+=500;wanted=0;state='complete';persist();render();setTimeout(()=>{state='find';render()},5000)}addEventListener('keydown',e=>{if(e.code==='KeyE'&&state==='find'){wanted=1;state='escape';persist();render()}if(e.code==='KeyM'&&state==='escape'&&wanted===0)reward()});setInterval(()=>{if(wanted>0&&state==='escape'){cooldown++;if(cooldown>=5){wanted--;cooldown=0;persist();render()}}},1000);render();