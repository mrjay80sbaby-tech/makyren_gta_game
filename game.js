import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.180.0/build/three.module.js';
const scene=new THREE.Scene();scene.background=new THREE.Color(0x91b7d3);scene.fog=new THREE.FogExp2(0x91b7d3,.0085);
const camera=new THREE.PerspectiveCamera(65,innerWidth/innerHeight,.1,1400);const renderer=new THREE.WebGLRenderer({antialias:true,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(devicePixelRatio,1.6));renderer.setSize(innerWidth,innerHeight);renderer.shadowMap.enabled=true;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.18;renderer.outputColorSpace=THREE.SRGBColorSpace;document.body.appendChild(renderer.domElement);
scene.add(new THREE.HemisphereLight(0xd9edff,0x19222b,2.1));const sun=new THREE.DirectionalLight(0xffe6bb,4.2);sun.position.set(-100,140,60);sun.castShadow=true;sun.shadow.mapSize.set(2048,2048);scene.add(sun);
const ground=new THREE.Mesh(new THREE.PlaneGeometry(1200,1200),new THREE.MeshStandardMaterial({color:0x26362c,roughness:.96}));ground.rotation.x=-Math.PI/2;ground.receiveShadow=true;scene.add(ground);const roadMat=new THREE.MeshStandardMaterial({color:0x11151a,roughness:.78});for(let x=-180;x<=180;x+=36){let r=new THREE.Mesh(new THREE.BoxGeometry(10,.12,1100),roadMat);r.position.set(x,.06,0);scene.add(r);for(let z=-500;z<500;z+=24){let l=new THREE.Mesh(new THREE.BoxGeometry(.18,.025,7),new THREE.MeshBasicMaterial({color:0xf0cf68}));l.position.set(x,.13,z);scene.add(l)}}for(let z=-180;z<=180;z+=36){let r=new THREE.Mesh(new THREE.BoxGeometry(1100,.12,10),roadMat);r.position.set(0,.06,z);scene.add(r)}
function building(x,z,w,d,h,c){const b=new THREE.Mesh(new THREE.BoxGeometry(w,h,d),new THREE.MeshStandardMaterial({color:c,roughness:.62,metalness:.1}));b.position.set(x,h/2,z);b.castShadow=b.receiveShadow=true;scene.add(b);const wm=new THREE.MeshBasicMaterial({color:0x9bd7ff});for(let y=3;y<h-2;y+=3)for(let i=0;i<Math.floor(w/3);i++){let q=new THREE.Mesh(new THREE.BoxGeometry(.72,.9,.035),wm);q.position.set(x-w/2+2+i*3,y,z-d/2-.025);scene.add(q)}}const colors=[0x17212b,0x242d37,0x343a40,0x153246,0x3a3029];for(let x=-160;x<=160;x+=24)for(let z=-160;z<=160;z+=24)if(x%36&&z%36)building(x,z,18+Math.random()*8,18+Math.random()*8,18+Math.random()*65,colors[Math.floor(Math.random()*colors.length)]);
const player=new THREE.Group(),skin=new THREE.MeshStandardMaterial({color:0x9a6549,roughness:.6}),shirt=new THREE.MeshStandardMaterial({color:0x111722,roughness:.46}),pants=new THREE.MeshStandardMaterial({color:0x151a23,roughness:.76});const torso=new THREE.Mesh(new THREE.CapsuleGeometry(.62,1.15,8,16),shirt);torso.position.y=2.15;torso.castShadow=true;player.add(torso);const neck=new THREE.Mesh(new THREE.CylinderGeometry(.19,.22,.28,16),skin);neck.position.y=3;player.add(neck);const head=new THREE.Mesh(new THREE.SphereGeometry(.49,32,20),skin);head.position.y=3.42;head.scale.set(1,.98,.92);player.add(head);const hair=new THREE.Mesh(new THREE.SphereGeometry(.51,28,16),new THREE.MeshStandardMaterial({color:0x120f0e,roughness:.92}));hair.position.set(0,3.67,-.015);hair.scale.set(1,.55,1);player.add(hair);for(const x of[-.32,.32]){let l=new THREE.Mesh(new THREE.CapsuleGeometry(.23,1.05,7,12),pants);l.position.set(x,.72,0);l.castShadow=true;player.add(l)}scene.add(player);
const vehicles=[];function car(x,z,color){const g=new THREE.Group();const body=new THREE.Mesh(new THREE.BoxGeometry(2.1,.65,4.3),new THREE.MeshStandardMaterial({color,metalness:.65,roughness:.28}));body.position.y=.72;body.castShadow=true;g.add(body);const cabin=new THREE.Mesh(new THREE.BoxGeometry(1.7,.65,2.1),new THREE.MeshStandardMaterial({color:0x111a22,roughness:.08,metalness:.15,transparent:true,opacity:.9}));cabin.position.y=1.25;g.add(cabin);const wheels=[];for(const dx of[-1.1,1.1])for(const dz of[-1.45,1.45]){let w=new THREE.Mesh(new THREE.CylinderGeometry(.34,.34,.2,20),new THREE.MeshStandardMaterial({color:0x070808,roughness:.85}));w.rotation.z=Math.PI/2;w.position.set(dx,.45,dz);g.add(w);wheels.push(w)}g.position.set(x,0,z);g.userData={speed:0,steer:0,wheels,drivable:true};scene.add(g);vehicles.push(g)}for(let i=0;i<30;i++)car((Math.random()-.5)*300,(Math.random()-.5)*300,[0x0d5c8b,0x8c2020,0xd3b84b,0xdddddd,0x101010][i%5]);
const keys={};addEventListener('keydown',e=>keys[e.code]=true);addEventListener('keyup',e=>keys[e.code]=false);let joy={x:0,y:0},sprint=false,velY=0,onGround=true;const stick=document.getElementById('stick'),knob=document.getElementById('knob');stick.addEventListener('pointermove',e=>{if(e.buttons!==1)return;const r=stick.getBoundingClientRect(),cx=r.left+r.width/2,cy=r.top+r.height/2,m=45;let x=e.clientX-cx,y=e.clientY-cy,d=Math.hypot(x,y);if(d>m){x=x/d*m;y=y/d*m}joy.x=x/m;joy.y=y/m;knob.style.transform=`translate(${x}px,${y}px)`});stick.addEventListener('pointerup',()=>{joy.x=joy.y=0;knob.style.transform=''});document.getElementById('jump').addEventListener('pointerdown',()=>{if(onGround&&!activeVehicle){velY=9;onGround=false}});document.getElementById('run').addEventListener('pointerdown',()=>sprint=!sprint);
let camYaw=Math.PI,camPitch=.18,drag=false,lastX=0,lastY=0;addEventListener('pointerdown',e=>{if(e.target===renderer.domElement){drag=true;lastX=e.clientX;lastY=e.clientY}});addEventListener('pointerup',()=>drag=false);addEventListener('pointermove',e=>{if(!drag)return;camYaw-=(e.clientX-lastX)*.006;camPitch=Math.max(-.15,Math.min(.7,camPitch-(e.clientY-lastY)*.004));lastX=e.clientX;lastY=e.clientY});
let activeVehicle=null;function nearestVehicle(){let best=null,bd=5;for(const v of vehicles){const d=v.position.distanceTo(player.position);if(d<bd){bd=d;best=v}}return best}function enterExit(){if(activeVehicle){player.visible=true;player.position.copy(activeVehicle.position);player.position.z+=3;activeVehicle.userData.speed=0;activeVehicle=null;return}const v=nearestVehicle();if(v){activeVehicle=v;player.visible=false;camera.position.y+=.4}}
addEventListener('keydown',e=>{if(e.code==='KeyE')enterExit()});const exitBtn=document.createElement('button');exitBtn.textContent='ENTER / EXIT';exitBtn.style.cssText='position:fixed;right:25px;bottom:35px;width:120px;height:48px;border-radius:14px;border:1px solid #ffffff33;background:#07101ddd;color:white;font-weight:800;z-index:5;display:block';document.body.appendChild(exitBtn);exitBtn.addEventListener('pointerdown',enterExit);
const clock=new THREE.Clock(),target=new THREE.Vector3(),camPos=new THREE.Vector3();function animate(){requestAnimationFrame(animate);const dt=Math.min(clock.getDelta(),.05);if(activeVehicle){const v=activeVehicle;const throttle=(keys.KeyW?1:0)-(keys.KeyS?1:0)-joy.y;const steer=(keys.KeyD?1:0)-(keys.KeyA?1:0)+joy.x;v.userData.speed+=throttle*18*dt;v.userData.speed*=Math.pow(.985,dt*60);v.userData.speed=THREE.MathUtils.clamp(v.userData.speed,-9,24);v.userData.steer=THREE.MathUtils.lerp(v.userData.steer,steer,.12);v.rotation.y+=v.userData.steer*v.userData.speed*.018*dt;v.translateZ(-v.userData.speed*dt);for(const w of v.userData.wheels)w.rotation.x-=v.userData.speed*dt*2.5;target.set(v.position.x,v.position.y+1.15,v.position.z);const cp=Math.cos(camPitch),sp=Math.sin(camPitch),dist=10;camPos.set(v.position.x+Math.sin(camYaw)*dist*cp,v.position.y+3.2+dist*sp,v.position.z+Math.cos(camYaw)*dist*cp);camera.position.lerp(camPos,.12);camera.lookAt(target)}else{let f=(keys.KeyW?1:0)-(keys.KeyS?1:0)-joy.y,r=(keys.KeyD?1:0)-(keys.KeyA?1:0)+joy.x;if(f||r){const speed=sprint?12:6,len=Math.hypot(f,r);f/=len;r/=len;const sy=Math.sin(camYaw),cy=Math.cos(camYaw),dx=r*cy+f*sy,dz=r*sy-f*cy;player.position.x+=dx*speed*dt;player.position.z+=dz*speed*dt;player.rotation.y=Math.atan2(dx,dz)}velY-=25*dt;player.position.y+=velY*dt;if(player.position.y<=0){player.position.y=0;velY=0;onGround=true}target.set(player.position.x,player.position.y+2.05,player.position.z);const cp=Math.cos(camPitch),sp=Math.sin(camPitch),dist=sprint?10:9;camPos.set(player.position.x+Math.sin(camYaw)*dist*cp,player.position.y+3.5+dist*sp,player.position.z+Math.cos(camYaw)*dist*cp);camera.position.lerp(camPos,.12);camera.lookAt(target)}renderer.render(scene,camera)}animate();setTimeout(()=>{const x=document.getElementById('loading');if(x){x.style.opacity=0;setTimeout(()=>x.remove(),700)}},900);addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});