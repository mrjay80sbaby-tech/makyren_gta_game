// MAKYREN-011 — NPC combat reactions + police tactical behavior
export function createNPCCombatAgent(npc,{police=false}={}){const a={state:'idle',health:100,cover:null,police,alert:0,cooldown:0,retreatTimer:0,update(dt,player){this.cooldown=Math.max(0,this.cooldown-dt);this.alert=Math.max(0,this.alert-dt);if(this.health<=0){this.state='down';return}if(this.retreatTimer>0){this.retreatTimer-=dt;this.state='retreat';return}if(!player){this.state='patrol';return}const dx=player.x-npc.position.x,dz=player.z-npc.position.z,d=Math.hypot(dx,dz);if(d<28){this.state=this.police?'engage':'alert';this.alert=2;if(this.police&&d<18&&this.cooldown<=0){this.cooldown=.7;this.state='fire'}if(this.health<35){this.state='retreat';this.retreatTimer=2.5}}else if(this.alert<=0)this.state='patrol';}};npc.userData.combatAgent=a;return a}
export function applyHitReaction(npc,damage,source){const a=npc.userData?.combatAgent;if(!a)return false;a.health=Math.max(0,a.health-damage);a.alert=4;if(a.health<=0)a.state='down';else if(a.police)a.state='engage';else a.state='flee';return true}
window.MakyrenNPCCombat={createNPCCombatAgent,applyHitReaction};