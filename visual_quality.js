// MAKYREN-023 — cinematic visual-quality layer; zero-cost procedural/mobile-first.
import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.180.0/build/three.module.js';
export function applyCinematicVisuals(renderer,scene,camera){const mobile=/Android|iPhone|iPad|iPod/i.test(navigator.userAgent)||innerWidth<900;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.12;renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.setPixelRatio(Math.min(devicePixelRatio,mobile?1.2:1.65));const pmrem=new THREE.PMREMGenerator(renderer);const sky=new THREE.Scene();const skyMat=new THREE.ShaderMaterial({side:THREE.BackSide,uniforms:{top:{value:new THREE.Color(0x19395c)},bottom:{value:new THREE.Color(0xb8d8e8)}},vertexShader:'varying vec3 v;void main(){v=position;gl_Position=projectionMatrix*modelViewMatrix*vec4(position,1.0);}',fragmentShader:'varying vec3 v;uniform vec3 top;uniform vec3 bottom;void main(){float h=normalize(v).y*.5+.5;gl_FragColor=vec4(mix(bottom,top,h),1.0);}'});sky.add(new THREE.Mesh(new THREE.SphereGeometry(700,32,16),skyMat));scene.add(sky);scene.fog=new THREE.FogExp2(0x8ca9b8,mobile?.010:.007);scene.traverse(o=>{if(o.isMesh){o.castShadow=true;o.receiveShadow=true;if(o.material?.isMeshStandardMaterial){o.material.envMapIntensity=.55;o.material.roughness=Math.min(.96,Math.max(.2,o.material.roughness));}}});pmrem.dispose();return{mobile,sky}}
export function addRoadDetail(scene){const asphalt=new THREE.MeshStandardMaterial({color:0x11151a,roughness:.88,metalness:.02});const curb=new THREE.MeshStandardMaterial({color:0x555b60,roughness:.75});for(let i=-12;i<=12;i++){const z=i*36;const c=new THREE.Mesh(new THREE.BoxGeometry(1100,.18,.65),curb);c.position.set(0,.12,z-5.4);scene.add(c);const a=new THREE.Mesh(new THREE.BoxGeometry(1100,.035,10),asphalt);a.position.set(0,.13,z);scene.add(a)}}
window.MakyrenVisuals={applyCinematicVisuals,addRoadDetail};